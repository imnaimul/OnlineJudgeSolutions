#include <iostream>
using namespace std;
using ulli = unsigned long long int;

int main() {
    int t;
    cin >> t;
    while(t--) {
        ulli l, r;
        cin >> l >> r;
        cout << l << " " << l * 2 << endl;
    }
    return 0;
}
